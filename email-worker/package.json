{
  "name": "email-worker",
  "version": "0.1.0",
  "description": "Email worker service with RabbitMQ consumption and Elastic APM instrumentation",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.18.2",
  "elastic-apm-node": "^4.4.0",
    "amqplib": "^0.10.3",
    "nodemailer": "^6.9.7",
    "pino": "^8.16.2"
  }
}
